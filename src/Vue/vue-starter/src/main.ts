import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

// The entry point is intentionally tiny: import global CSS, then mount the root component.
createApp(App).mount('#app')
