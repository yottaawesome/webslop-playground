# Basic Minimal API

Just a sample on using ASP.NET Minimal API, Microsoft's successor to ASP.NET Web API. It doesn't appear all that much has changed between the two, though Minimal API is quite a bit cleaner.
